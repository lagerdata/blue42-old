kind: pipeline
type: docker
name: default

steps:
- name: build
  image: adhanali/blue42
  commands:
  - mkdir -p _build
  - cd _build
  - cmake .. -G Ninja -DCMAKE_TOOLCHAIN_FILE=../cmake/arm-gcc-toolchain.cmake
  - cmake --build .
